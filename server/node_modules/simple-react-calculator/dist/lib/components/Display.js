import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";
import styled from "@emotion/styled";
import { useFontResize } from "../hooks/useFontResize";
export const Display = React.memo((props) => {
    const { divRef, spanRef } = useFontResize(props.value);
    return (_jsx(StyledDiv, Object.assign({ ref: divRef }, { children: _jsx(StyledSpan, Object.assign({ ref: spanRef }, { children: props.value }), void 0) }), void 0));
});
const StyledDiv = styled.div `
  width: 100%;
  height: 20%;
  background-color: #323232;
  font-size: 14vmin;
  color: white;
  display: flex;
  align-items: flex-end;
  flex-direction: column-reverse;
`;
const StyledSpan = styled.span `
  margin: 16px;
`;
