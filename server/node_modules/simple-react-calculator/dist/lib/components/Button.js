import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";
import styled from "@emotion/styled";
export const ButtonBackgroundColor = {
    Black: "#434343",
    Gray: "#5d5e5e",
    Orange: "#f2a23c",
};
export const Button = React.memo((props) => {
    return (_jsx(StyledButton, Object.assign({ backgroundColor: props.backgroundColor, isLarge: props.isLarge, isSelected: props.isSelected, onClick: props.onClick, value: props.value }, { children: props.value }), void 0));
});
const StyledButton = styled.button `
  background-color: ${(props) => props.backgroundColor};
  color: white;
  width: ${(props) => (props.isLarge ? "50%" : "25%")};
  height: 16%;
  font-size: 6vmin;
  border: ${(prop) => prop.isSelected ? "3px solid black" : "1px solid black"};
  padding-left: ${(props) => (props.isLarge ? "12%" : "")};
  text-align: ${(props) => (props.isLarge ? "left" : "")};

  :focus {
    outline: 0px;
  }

  :active {
    opacity: 0.5;
  }
`;
